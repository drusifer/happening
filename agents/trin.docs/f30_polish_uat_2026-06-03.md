# UAT Report: F-30 Polish & Linter Cleanup
**Trin — 2026-06-03**

## Verdict: PASS

## Verified Items
1. **Hoisted `onWeakMatch` in `DisplayService`**:
   - Verified that `onWeakMatch` is passed in to `DisplayService` and configured to report telemetry.
   - Added unit test: `hoisted onWeakMatch is triggered on weak match during refresh` passes.
2. **`DisplayService.setPersistedChoice`**:
   - Swapped resolver construction in `settings_panel.dart` to use `setPersistedChoice`.
   - Verified that selecting a display in SettingsPanel persists the choice and changes the resolver.
   - Added unit test: `setPersistedChoice swaps resolver and triggers refresh using hoisted callback` passes.
3. **Single Resolution Refresh Bug**:
   - `DisplayService._refresh` now resolves the choice exactly once per refresh.
   - Verified that `onWeakMatch` is called exactly once (fixing the previous issue where it was executed twice).
4. **Integration Test Compilation**:
   - Mocked `DisplayService` passed to `WindowService` in `integration_test/timeline_strip_test.dart`.
   - Verified compilation passes.
5. **Linter & Analyzer Warnings**:
   - Fixed all unused `super.key` parameters, unnecessary `dart:ui` imports, const constructor warnings, and empty block warnings.
   - `make lint` passes with 0 issues.

## Suite Status
- Total passing tests: 419
- Golden failure: 1 (pre-existing golden `hover_card_alignment` test, unrelated)
