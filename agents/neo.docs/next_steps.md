# Neo Next Steps — 2026-06-03

## Ready for verification
1. Trin UAT of F-30 polish updates (`setPersistedChoice` + callback hoisting + linter clean + settings select fix + primary classify fix + window movement fix + fallback index uniqueness + fallback index contiguous numbering).
2. Morpheus review of F-30 polish changes.

## Blocked on hardware (Drew)
- F30-C3: Windows AppBar secondary-monitor manual verify.
- F30-F1: Multi-platform UAT (X11, Wayland, Windows, macOS).
- F30-F2: Smith real-app UX pass.
- F30-F3: Oracle docs update.
