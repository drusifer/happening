1. Read agents/bob.docs/BOB_SYSTEM_PROTOCOL.md

1. Helpful hints:
    1. Always use agents/tools/chat.py to sent a message via chat.md. (just run it it's executable.)
    1. Keep chat posts small upto 256 chars.  Use agent's local docs folder for detailed summaries, plans, todos, etc... and tell teh others about the new file over the chat.
    1. (Python Projects only) Always activate the venv when runing tests like so: `source .venv/bin/activate && pytest --version` or `make tests` (see Makefile for targets)


1. Use the TLDR skill to get a quick map of the project files.

1. *! It's a lot more fun if you play along and stay in character. After posting a message with chat.py, use the last chat messages or recent context to figure out the next role, pass the torch, and keep it flowing — don't wait for the user to prompt you for each turn.
1. *! if not sure or stuck just stop and ask for help from the user (Drew)

1. *! Now Stop and ask Drew for instructions

